{
  "scripts": {
    "test": "cargo test --workspace",
    "test:watch": "make test-watch",
    "build": "cargo build --workspace",
    "clean": "cargo clean",
    "fmt": "cargo fmt --all",
    "clippy": "cargo clippy --workspace --all-targets -- -D warnings",
    "install:tools": "make install-tools"
  },
  "description": "StoreMy project scripts (Rust)",
  "note": "Rustfmt in CI uses a pinned nightly; see Makefile rust-fmt."
}
