## What changed and why

<!-- One paragraph: what problem does this solve, or what capability does it add? -->

## How to test

<!-- How did you verify this works? e.g. `cargo nextest run -p storemy --lib <filter>`, manual REPL, etc. -->

## Checklist

- [ ] `make check` passes locally (`fmt` + `clippy` + `ci-test`)
- [ ] New public items have doc comments
- [ ] Roadmap / CLAUDE.md updated if this closes a phase milestone
