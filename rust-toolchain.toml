[toolchain]
# Edition 2024 requires 1.85+. Pin for reproducible CI and local builds.
# 1.88+: let chains (2024 edition); 1.89+ required by cargo-audit / smol_str 0.3.6.
channel = "1.89.0"
profile = "default"
components = ["rustfmt", "clippy"]
